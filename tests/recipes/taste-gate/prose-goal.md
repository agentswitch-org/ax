Rewrite the deliverable into flat, factual developer-documentation prose that
describes what the tool is and what it does. Keep every technical fact: it is a
session switcher, it runs tasks in tracked background windows, it does not require
a terminal multiplexer but can use one, it gives sessions meaningful names, and it
reports session state. Do not add facts. Do not add features that are not stated.
